[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "math-stats-notes"
version = "0.1.0"
description = "Mathematical and statistical utilities for learning and experimentation"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]